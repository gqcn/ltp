# LTP Frontend

`apps/frontend`是`LTP`的`React`控制台，使用`TypeScript`、`Vite`和`Tailwind CSS`。

## 命令

在仓库根目录执行：

```bash
make dev
```

或在本目录执行：

```bash
pnpm install
pnpm dev
```

开发服务监听`http://127.0.0.1:5173`，并将`/api`代理到宿主的`8000`端口。
