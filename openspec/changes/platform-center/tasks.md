## 1. 数据与本地 LDAP

- [x] 1.1 新增`002-platform-center.sql`：扩展`sys_user`，创建角色/团队/成员/`LDAP`配置表，种子内置角色、默认`LDAP`配置、`algo`/`sre`平台用户
- [x] 1.2 新增 mock 数据：其余原型目录用户对应的平台用户与示例团队
- [x] 1.3 在`hack/deploy`增加 Docker 模拟`LDAP`、种子 LDIF 与`make ldap.up`
- [x] 1.4 执行`make db.init`与`make dao`，把新表纳入`gfcli`生成清单

## 2. LDAP 与认证

- [x] 2.1 实现`ldap`服务：配置读写、测试连接、目录检索、用户查找与绑定；`Directory`接口可注入替身
- [x] 2.2 扩展登录契约（`mode`）与会话 DTO（角色、菜单、来源、管理员标记），并实现`LDAP`/管理员登录分流
- [x] 2.3 扩展`bizctx`与权限中间件，按`permission`前缀校验菜单；数据中心路由接入该中间件
- [x] 2.4 补充认证与`LDAP`替身单元测试

## 3. 平台中心后端

- [x] 3.1 定义用户/角色/团队/系统配置 API，执行`make ctrl`并实现控制器转换
- [x] 3.2 实现用户服务：列表批量投影、从`LDAP`添加、批量启停/授权/移除
- [x] 3.3 实现角色服务：列表批量用户数、改名
- [x] 3.4 实现团队服务：创建/编辑、成员增删；不提供队列关联
- [x] 3.5 补充用户、角色、团队服务单元测试

## 4. 控制台前端

- [x] 4.1 登录页提交`mode`并打通`LDAP`登录；会话驱动落地页
- [x] 4.2 壳层按菜单 ∩ 已启用模块渲染平台中心入口，补空态页与面包屑
- [x] 4.3 实现用户管理页：筛选分页、从`LDAP`添加、批量/单条授权与启停移除
- [x] 4.4 实现角色管理页与系统配置`LDAP`页（保存、测试连接）
- [x] 4.5 实现团队管理页：列表详情、新建/编辑、成员维护；隐藏队列入口
- [x] 4.6 更新根`README.md`与部署说明：`LDAP` Docker、演示账号与模块范围
- [x] 4.7 系统配置页在`1440×900`下与原型一致，不出现窗口右侧滚动条（反馈修复；无 i18n 影响）

## 5. 验证

- [x] 5.1 运行`Go`测试与`make lint`
- [x] 5.2 更新现有登录 E2E，并新增平台中心与`LDAP`角色 E2E
- [x] 5.3 启动本地服务与模拟`LDAP`，在浏览器走通管理员与`LDAP`闭环

## 规则影响记录

- 前端视觉以`prototype.v4`为准（`.agents/rules/frontend-ui.md`缺失）。
- 运行时文案使用中文，不引入语言包（`.agents/rules/i18n.md`缺失）。
- 本迭代无行级数据权限（`.agents/rules/data-permission.md`缺失，判定无影响）。
- 不引入字典模块；枚举使用 Go 命名类型。
