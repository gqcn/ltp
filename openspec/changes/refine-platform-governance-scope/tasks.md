## 1. 模块边界收敛

- [x] 1.1 将数据工程规划重写为第三方数据工程系统提供、训练平台只读消费引用的边界说明，并更新文件名
- [x] 1.2 将平台治理章节重写为服务合并、基础审计、资源容量与使用统计、使用监控和优化建议，并更新文件名

## 2. 规划一致性

- [x] 2.1 更新规划总览中的架构边界、模块概览、功能排期和季度路线
- [x] 2.2 更新工作流及其它模块对子平台数据工程、复杂角色或开放能力的直接引用
- [x] 2.3 更新行业参考映射，确保外部系统承接能力与训练平台建设能力分离

## 3. 验证

- [x] 3.1 检查`docs/平台规划/`内链接、旧模块名、已取消功能名和数据工程残留承诺
- [x] 3.2 运行`openspec validate refine-platform-governance-scope --strict`并记录规则域无影响判断与文档验证结果

验证记录：重命名后的`3.6`与`3.7`链接均可解析，旧模块名、已取消功能名和训练平台承担数据工程的建设承诺扫描无残留，`git diff --check`通过。全量相对链接扫描另发现既有的`../ops/fastx-alert-webhook.md`目标不存在，该链接不在本变更修改范围内，未作处理。

规则域记录：文档、`OpenSpec`与架构规则域命中，已在本次实施前读取并遵守；后端、接口、数据库与测试规则域判定为无影响，因为本变更不修改生产代码、接口契约、数据库、测试或运行行为。`openspec validate refine-platform-governance-scope --strict`验证通过。

## 4. 反馈修订

反馈原因：独立“训练数据引用”章节没有自身业务生命周期，容易放大训练平台的数据职责；“任务提交与观测”以具体动作命名，无法稳定概括任务完整生命周期，也容易让读者误判相关能力尚未完成。

第二轮反馈原因：第2.1节的模型产物交付介绍被质量证据等附属信息分散了重点，工作流与自动化介绍又套用了审批和质量证据收集链路，没有直接说明标准化交付以及重复、易错人工操作自动化这两个核心价值。

- [x] 4.1 更新提案、设计与增量规范，记录训练数据引用并入任务模块及模块更名决策
- [x] 4.2 将“任务提交与观测”重命名为“训练任务管理”，并合并训练数据引用、任务快照和职责边界
- [x] 4.3 删除独立训练数据引用模块，顺延后续模块编号并同步总览、排期、路线和行业映射
- [x] 4.4 检查旧模块名、文件链接和编号一致性，运行严格验证并记录反馈修订结果
- [x] 4.5 修正第2.1节模型产物交付与工作流自动化的核心定位，并检查介绍中不再使用审批或质量证据收集作为主线

反馈验证记录：`docs/平台规划/`已统一使用“训练任务管理”，不再保留独立“训练数据引用”模块或旧文件链接；模块文件与一级标题按`3.1`至`3.11`连续编号，总览、功能排期、季度路线和行业映射已同步。`openspec validate refine-platform-governance-scope --strict`与`git diff --check`验证通过。全量相对链接中的既有`../ops/fastx-alert-webhook.md`目标仍不存在，该链接不属于本次反馈修改范围，继续保留原记录。

第二轮反馈验证记录：第2.1节“模型产物交付”已聚焦标准化、规范化交付逻辑及模型资产系统对接，“工作流与自动化”已聚焦将稳定、重复且易错的人工操作沉淀为可复用自动化流程；两项介绍均不再以审批或质量证据收集作为主线。`openspec validate refine-platform-governance-scope --strict`与`git diff --check`验证通过。

反馈规则域记录：文档、`OpenSpec`与架构规则域命中，已在反馈实施前重新读取并遵守；后端、接口、数据库与测试规则域无影响，因为反馈只调整规划信息结构、模块名称和文档编号，不修改生产代码、接口契约、数据库、测试或运行行为。
