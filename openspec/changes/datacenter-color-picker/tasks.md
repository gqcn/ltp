## 1. 前端

- [x] 1.1 将数据中心展示色改为色块切换 + 输入框色板 + 手填 hex
- [x] 1.2 补充样式，保证弹窗内色板不被裁切、浅色上图标可读

## 2. 验证

- [x] 2.1 新增 E2E：图标切换、色板点选、手填并保存
- [x] 2.2 浏览器走通新建/编辑选色

## 规则影响记录

- 无 API / SQL / 后端变更。
- 无 i18n 语言包；`aria-label`用中文（`.agents/rules/i18n.md`缺失）。
- `.agents/rules/frontend-ui.md`缺失，交互对齐 GitHub 标签选色与现有表单样式。
