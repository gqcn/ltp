## 1. 开发机与训练任务边界

- [x] 1.1 将开发机工作空间调整为默认不申请`GPU`，同步资源准入、生命周期和空闲回收说明
- [x] 1.2 更新训练命令行与训练任务管理，明确`CLI`创建独立训练任务并重新申请`GPU`

## 2. 规划一致性

- [x] 2.1 更新规划总览中的模块概览、功能排期、季度路线和行业映射
- [x] 2.2 检查开发机、训练任务、资源队列和数据中心描述，确保没有新增权限或资源池抽象

## 3. 验证

- [x] 3.1 扫描开发机长期占卡、创建时申请卡数、`GPU`利用率回收和动态挂卡等冲突承诺
- [x] 3.2 运行`openspec validate separate-development-training-resources --strict`与`git diff --check`，记录规则域无影响判断和文档验证结果

验证记录：开发机创建时申请卡数、依赖`GPU`利用率判定空闲以及复用开发机进程直接训练等建设承诺扫描无残留；默认`GPU`请求为`0`、`CLI`创建独立训练任务、任务重新申请`GPU`和生命周期分离的口径已在模块文档、总览、排期、路线及行业映射中保持一致。`openspec validate separate-development-training-resources --strict`与`git diff --check`验证通过。全量相对链接扫描仍只有既有`../ops/fastx-alert-webhook.md`目标不存在，该链接不在本变更修改范围内。

规则域记录：文档、`OpenSpec`与架构规则域命中，已在实施前读取并遵守；后端、接口、数据库与测试规则域无影响，因为本变更不修改生产代码、接口契约、数据库、测试或运行行为。
